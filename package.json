{
  "name": "neutv",
  "version": "2.0.0",
  "private": true,
  "description": "NEU TV (Central Stream). backend/ is the API, frontend/ is the app.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "npm start   --prefix backend",
    "seed:videos": "npm run seed:videos --prefix backend --",
    "test": "npm test     --prefix backend",
    "eval": "npm run eval --prefix backend",
    "verify": "npm run verify --prefix backend",
    "admin": "npm run dev --prefix admin",
    "admin:build": "npm run build --prefix admin",
    "admin:start": "npm start --prefix admin",
    "web": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "web:start": "npm start --prefix web",
    "portal": "npm run dev --prefix portal",
    "portal:build": "npm run build --prefix portal",
    "portal:start": "npm start --prefix portal",
    "admin:create": "npm run admin:create --prefix backend --"
  }
}
